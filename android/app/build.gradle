plugins {
    id "com.android.application"
    id 'com.google.gms.google-services' // This should be here
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin" // This must be last
}

android {
    namespace = "com.example.sendora"
    compileSdk = 35
    ndkVersion = "27.0.12077973"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.sendora"
        minSdk = 23
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // Add your own signing config for the release build if needed.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
